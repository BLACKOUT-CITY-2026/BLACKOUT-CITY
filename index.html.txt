<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blackout City - Applications Form</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        body {
            background-color: #0b0b0b;
            color: #f1f1f1;
            padding: 20px;
        }
        nav {
            display: flex;
            justify-content: center;
            gap: 30px;
            padding: 20px;
            background: rgba(0,0,0,0.8);
            border-bottom: 1px solid #222;
            margin: -20px -20px 30px -20px;
        }
        nav a {
            color: #aaa;
            text-decoration: none;
            font-size: 13px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: 0.2s;
        }
        nav a:hover, nav a.active {
            color: #fff;
            border-bottom: 2px solid #e71d36;
            padding-bottom: 2px;
        }
        .container {
            max-width: 750px;
            margin: 20px auto;
            background: #141414;
            padding: 40px;
            border-radius: 8px;
            border-top: 4px solid #e71d36;
            box-shadow: 0 8px 24px rgba(0,0,0,0.6);
        }
        h1 {
            color: #e71d36;
            margin-bottom: 5px;
            font-size: 24px;
            text-transform: uppercase;
        }
        p.subtitle {
            color: #888;
            font-size: 13px;
            margin-bottom: 25px;
            text-transform: uppercase;
        }
        .nav-tabs {
            display: flex;
            gap: 10px;
            margin-bottom: 30px;
            border-bottom: 1px solid #222;
            padding-bottom: 15px;
            flex-wrap: wrap;
        }
        .tab {
            background: #1e1e1e;
            color: #fff;
            border: 1px solid #333;
            padding: 10px 20px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 12px;
            text-transform: uppercase;
            font-weight: bold;
            transition: 0.2s;
        }
        .tab.active, .tab:hover {
            background: #e71d36;
            border-color: #e71d36;
        }
        .form-section {
            display: none;
        }
        .form-section.active {
            display: block;
        }
        .form-group {
            margin-bottom: 20px;
        }
        label {
            display: block;
            font-size: 12px;
            font-weight: bold;
            color: #ccc;
            margin-bottom: 8px;
            text-transform: uppercase;
        }
        input[type="text"], textarea {
            width: 100%;
            padding: 12px;
            background: #1e1e1e;
            border: 1px solid #333;
            border-radius: 4px;
            color: #fff;
            font-size: 14px;
        }
        input:focus, textarea:focus {
            border-color: #e71d36;
            outline: none;
        }
        textarea {
            resize: vertical;
            height: 100px;
        }
        .submit-btn {
            background-color: #e71d36;
            color: white;
            border: none;
            padding: 14px 25px;
            font-size: 14px;
            font-weight: bold;
            border-radius: 4px;
            cursor: pointer;
            text-transform: uppercase;
            width: 100%;
            transition: background 0.2s;
        }
        .submit-btn:hover {
            background-color: #c5162d;
        }
    </style>
</head>
<body>

<div class="container">
    <nav>
        <a href="index.html">Home</a>
        <a href="#">Events</a>
        <a href="#">Rules</a>
        <a href="apply.html" class="active">Applications</a>
        <a href="#">Team</a>
        <a href="#">Streamers</a>
    </nav>

    <h1>BLACKOUT CITY APPLICATIONS</h1>
    <p class="subtitle">Select a department to submit your official application directly to the staff team.</p>

    <!-- Tabs Navigation -->
    <div class="nav-tabs">
        <div class="tab active" onclick="switchTab(event, 'whitelist')">Whitelist</div>
        <div class="tab" onclick="switchTab(event, 'police')">Police</div>
        <div class="tab" onclick="switchTab(event, 'ems')">EMS</div>
        <div class="tab" onclick="switchTab(event, 'gang')">Gang & Family</div>
    </div>

    <!-- 1. Whitelist Form -->
    <div id="whitelist" class="form-section active">
        <form>
            <div class="form-group">
                <label>Character Name (First Last) *</label>
                <input type="text" placeholder="e.g. John Smith" required>
            </div>
            <div class="form-group">
                <label>Age (OOC / IC) *</label>
                <input type="text" placeholder="Real Age / Character Age" required>
            </div>
            <div class="form-group">
                <label>Server Rules Knowledge *</label>
                <textarea placeholder="Do you have good knowledge of rules like RDM/FailRP?" required></textarea>
            </div>
            <div class="form-group">
                <label>Character Backstory *</label>
                <textarea placeholder="Briefly explain your character's story..." required></textarea>
            </div>
            <button type="submit" class="submit-btn">Submit Whitelist Application</button>
        </form>
    </div>

    <!-- 2. Police Form -->
    <div id="police" class="form-section">
        <form>
            <div class="form-group">
                <label>Character Name *</label>
                <input type="text" placeholder="First Last" required>
            </div>
            <div class="form-group">
                <label>Why do you want to join the Police? *</label>
                <textarea placeholder="Your motivation and character background..." required></textarea>
            </div>
            <div class="form-group">
                <label>Law Enforcement / LEO Experience *</label>
                <textarea placeholder="Describe your past police experience..." required></textarea>
            </div>
            <div class="form-group">
                <label>Weekly Availability *</label>
                <input type="text" placeholder="e.g. Mon-Fri 6pm-11pm EST" required>
            </div>
            <button type="submit" class="submit-btn">Submit Police Application</button>
        </form>
    </div>

    <!-- 3. EMS Form -->
    <div id="ems" class="form-section">
        <form>
            <div class="form-group">
                <label>Character Name *</label>
                <input type="text" placeholder="First Last" required>
            </div>
            <div class="form-group">
                <label>Why do you want to join EMS? *</label>
                <textarea placeholder="Your motivation for medical RP..." required></textarea>
            </div>
            <div class="form-group">
                <label>Medical / EMS Experience *</label>
                <textarea placeholder="Describe your past medical RP experience..." required></textarea>
            </div>
            <button type="submit" class="submit-btn">Submit EMS Application</button>
        </form>
    </div>

    <!-- 4. Gang Form -->
    <div id="gang" class="form-section">
        <form>
            <div class="form-group">
                <label>Gang Name *</label>
                <input type="text" placeholder="Official organization name" required>
            </div>
            <div class="form-group">
                <label>Members' Discord IDs (8+) *</label>
                <textarea placeholder="At least 8 Discord IDs required (one per line)..." required></textarea>
            </div>
            <div class="form-group">
                <label>Origin Story of the Gang *</label>
                <textarea placeholder="Origin story..." required></textarea>
            </div>
            <div class="form-group">
                <label>Gang Objective *</label>
                <textarea placeholder="Primary criminal/business objectives..." required></textarea>
            </div>
            <button type="submit" class="submit-btn">Submit Gang Application</button>
        </form>
    </div>

</div>

<script>
    function switchTab(evt, tabName) {
        document.querySelectorAll('.form-section').forEach(sec => sec.classList.remove('active'));
        document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
        
        document.getElementById(tabName).classList.add('active');
        evt.currentTarget.classList.add('active');
    }
</script>

</body>
</html>
